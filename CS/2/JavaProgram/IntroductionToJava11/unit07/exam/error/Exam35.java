/*
    System.out.print("Do you want to guess anther word? [Y/n]");
    yn = input.nextChar();
    if(yn != 'Y' || yn != 'y')
        break;
*/

/*
    System.out.print("Enter the length of word you want to guess: ");

    int len = input.nextInt();
*/

/*
    for(int i = 0; i < len; i++){
        if(answer.charAt(i) == userInput){
            if(sign.charAt(i) == userInput){
                already = 1;
                break;
            }
            else{
                sign[i] = c;
            }
        }
    }
*/
